/// Layout and sizing constants. Enforces UX rules project-wide.
abstract final class AppSizes {
  /// Minimum tap target size — 80×80 logical pixels. Non-negotiable.
  static const double tapMin      = 80.0;
  /// Preferred primary button size.
  static const double buttonWidth = 160.0;
  static const double buttonHeight = 80.0;
  /// Card corner radius.
  static const double radiusCard  = 20.0;
  static const double radiusButton = 40.0;
  /// Standard spacing scale.
  static const double spaceXS = 4.0;
  static const double spaceS  = 8.0;
  static const double spaceM  = 16.0;
  static const double spaceL  = 24.0;
  static const double spaceXL = 40.0;
}
